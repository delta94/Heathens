import CircularProgress from '@material-ui/core/CircularProgress';

const Preloader = () =>
{
    return (
        <div id="preloader">
            <CircularProgress />
        </div>
    );
};

export default Preloader;
